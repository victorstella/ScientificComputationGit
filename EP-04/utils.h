#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <sys/time.h>

#define ABS(num)  ((num) < 0.0 ? -(num) : (num))

// Retorna tempo em segundos
double timestamp(void);

